programa fib_rec;
vars
  x : entero;
entero fib( n : entero )
{
  si (n < 2) {
    retorna n;
  } sino {
    retorna fib(n - 1) + fib(n - 2);
  }
}
inicio
{
  x = fib(7);
  escribe("fib recursivo 7 = ", x);
}
fin
